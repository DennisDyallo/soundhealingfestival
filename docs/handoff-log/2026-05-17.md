# 2026-05-17 Handoff Summary

## Current State

The repo is clean and the latest work is committed on `main`.

Latest commits:

- `d71b20f` record handoff log commit for `REQ-20260516-002`.
- `cc4764f` save the May 16 Codex handoff log.
- `5649639` record the content guidance commit for `REQ-20260516-001`.
- `fca9c98` add consistent content-change guidance and `/DevMode`.
- `5c278c8` close the review-fix request.
- `89fdee4` enforce request-log coverage for `.codex/` operating files.

## Operating Model

- Mateusz is the default non-technical product owner.
- Dennis can write `/DevMode` to switch Codex into technical consultant communication.
- `AGENTS.md` is the canonical Codex operating contract.
- `$soundhealing-owner-ops` is the single owner-facing skill.
- `.codex/agents/product-owner-operator.md` handles routine owner requests.
- `.codex/agents/release-reviewer.md` handles release-readiness review.

## Content Consistency Rules

Codex should help Mateusz add or edit content without visual surprises by:

- Reusing existing components before creating new markup.
- Reusing `static/design-tokens.css`.
- Checking `/styleguide`, nearby sections, and existing styleguide components before adding posts, pages, or sections.
- Avoiding ad hoc colors, typography, spacing, buttons, cards, and layout patterns.
- Adding styleguide examples when a genuinely new reusable pattern is introduced.
- Running visual checks for layout-sensitive changes.
- Reporting exactly what Mateusz should review.

## Guardrails

- Request-log guardrails now include `.codex/`.
- Changes to `.codex/`, `src/`, `static/`, `scripts/`, or root `docs/*.md` require request-log updates.
- `scripts/scripts-smoke.test.ts` includes coverage proving `.codex/` changes require request-log entries.
- Completed request logs now reference real commits and rollback refs.

## Recommended Next Work

1. Fix public-facing factual drift before production:
   - SEO title still references `15 Februar 2026`.
   - Page content references 2027.
   - Ticket CTA state should be made consistent.
2. Decide whether to keep content in migrated Wix HTML for now or refactor event facts into structured content.
3. Before adding CMS/backend, keep the static SvelteKit deployment and Codex/Git workflow unless Mateusz has repeated editing needs that justify a CMS.
4. When new pages/posts become recurring, add recipes first; create new skills only after repeated real usage proves the need.

## Validation Baseline

Most recent validation from the workflow changes:

- `npm run ci:check` passed.
- `npm run request-log:verify` passed.
- Skill validation passed.

